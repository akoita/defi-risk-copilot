# Copy to .env and fill in your values
PROJECT_ID=your-gcp-project
BQ_LOCATION=US
DATASET=risk_copilot
# BigQuery connection name for Vertex AI remote models (created via UI or CLI)
# Example format: PROJECT_ID.US.vertex_us
CONNECTION_NAME=US.vertex_us
# Optional service account (must have Vertex AI + BigQuery perms)
SERVICE_ACCOUNT=service-account@your-gcp-project.iam.gserviceaccount.com
